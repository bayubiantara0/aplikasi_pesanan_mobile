import 'package:flutter/material.dart';

class RiwayatPesanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Riwayat Pesan'),
        backgroundColor: Colors.green,
      ),
      body: Center(
        child: Text('Halaman Riwayat Pesan'),
      ),
    );
  }
}
